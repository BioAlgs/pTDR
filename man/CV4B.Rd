% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shared-funs.R
\name{CV4B}
\alias{CV4B}
\title{Cross-Validation for Bandwidth Selection}
\usage{
CV4B(y, x, Bmat, h, m = 10, group = NULL)
}
\arguments{
\item{y}{Numeric vector of response variables.}

\item{x}{Numeric matrix of predictor variables.}

\item{Bmat}{Transformation matrix applied to predictors before regression.}

\item{h}{Bandwidth parameter for local polynomial regression.}

\item{m}{Number of groups for cross-validation (default is 10).}

\item{group}{Optional vector specifying the grouping of observations.
If \code{NULL}, observations are randomly divided into groups.}
}
\value{
A list containing:
\itemize{
\item{group}{Vector indicating the group assignment of each observation.}
\item{CVscore}{Numeric vector of cross-validation scores for each group.}
\item{CV}{Mean of the cross-validation scores.}
\item{CVstd}{Standard deviation of the cross-validation scores.}
}
}
\description{
Performs m-fold cross-validation to select the optimal bandwidth for local polynomial regression.
The function divides the data into \code{m} groups, either randomly or based on a provided grouping,
and calculates the cross-validation score for the given bandwidth \code{h}.
}
\examples{
# Simulated data
set.seed(123)
y <- rnorm(100)
x <- matrix(rnorm(200), ncol = 2)
Bmat <- diag(2)
h <- 1

# Perform cross-validation
cv_results <- CV4B(y, x, Bmat, h)
print(cv_results)

}
