% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pTDR.R
\name{pTDR}
\alias{pTDR}
\title{Tensor-based Sliced Inverse Regression (SIR)}
\usage{
pTDR(y, x, p, nslice, ndir = 1, maxiter = 200, tol = 1e-08, eps = 1e-08)
}
\arguments{
\item{y}{Numeric vector of response variables, indicating the outcome for each observation.}

\item{x}{Numeric matrix of predictor variables, where each row is an observation
and columns correspond to variables, possibly representing a flattened tensor.}

\item{p}{Numeric vector indicating the dimensions of the tensor representation
for the predictor variables. The product of \code{p} should equal the number of columns in \code{x}.}

\item{nslice}{Integer specifying the number of slices for the SIR analysis.}

\item{ndir}{Integer indicating the number of directions to estimate (default is 1).}

\item{maxiter}{Maximum number of iterations for the algorithm (default is 200).}

\item{tol}{Tolerance level for convergence in the optimization algorithm (default is 1e-8).}

\item{eps}{A small numerical value to stabilize computations (default is 1e-8).}
}
\value{
A list containing the following components:
\itemize{
\item p: The dimensions of the tensor as specified by the input.
\item ndir: The number of directions estimated.
\item info: Information from the initial SIR analysis, including slice information.
\item ans: Results from the tensor-based SIR analysis, varying based on the dimensionality of \code{p}.
}
}
\description{
Implements a tensor-based approach to Sliced Inverse Regression (SIR) for
dimensionality reduction in supervised learning scenarios. This function
can handle tensor inputs of up to 3 dimensions, specified by the \code{p} parameter.
}
\examples{
set.seed(123)
y <- rnorm(100)
x <- matrix(rnorm(300), ncol = 3)
p <- c(1, 3)
nslice <- 10

result <- pTDR(y, x, p, nslice)
print(result)

}
